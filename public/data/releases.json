{
  "releases": [
    {
      "year": "v0.40.0",
      "title": "Two Levels Up",
      "description": "<p><strong>The inlined binding engine moves from 0.5.1 to 1.0.0, and brings two context names with it.</strong> No new Domma API of its own; nothing that already worked has changed.</p><p><strong><code>$parents</code></strong> is the whole ancestor chain, nearest first. <code>$parent</code> is one step out, and until now it was the only step there was - from inside a list inside a list, <strong>two levels up could not be reached at all</strong>: a bare name is the item, <code>$parent</code> is the group, and the root was simply unreachable. <code>$parents[0]</code> is <code>$parent</code> and <code>$parents[1]</code> reads a grandparent with no <code>$data</code> in sight. It is built only if a template asks for it, by walking the chain on first read, so a list that never mentions the name pays nothing for it.</p><p><strong><code>$parentContext</code></strong> is the enclosing <em>context</em> rather than its data - the one name here that is a context. It exists because ancestor data cannot answer <em>which row of the outer list am I in?</em>: position lives on the context, not on the item, so no amount of <code>$parents</code> reaches it. <code>$parentContext.$index</code> answers it, and nothing could before. <code>$parent</code> stays data and not a context, because making it one would mean writing <code>$parent.$data.name</code> everywhere.</p><p><strong>A list inside a list is <code>{{#each}}</code>, not a second <code>data-each</code>.</strong> Always true, and it never said anything. <code>data-each</code> is the <code>applyBindings</code> spelling and the compiler discovers lists from <code>{{#each}}</code> only - and a list's item template <em>is</em> compiled markup. So a nested <code>data-each</code> was left exactly as written: its body rendered <strong>once</strong>, as ordinary markup, with every binding in it resolving against the <strong>outer</strong> item. It looks close enough to working to survive review, which is how it survived. Activation now warns, quotes the expression back and names the form to write instead.</p><p>Also arriving with the pin: a binding write to a <strong>frozen</strong> target warns and skips instead of throwing. It was reachable long before either name existed - <code>data-model</code> against a frozen view model has always landed there - and a binding that throws takes the page down with it, which is the one thing this layer promises never to do.</p><p><strong>Known limitation - <code>$parentContext.$index</code> does not survive a reorder.</strong> Appending to the outer list is fine, and so is anything that leaves existing positions alone. <em>Moving</em> an outer row is not: a sort, a reverse or a prepend relocates the DOM correctly and leaves every inner item reading the position its group used to be in. Ancestor data is correct throughout; it is only the enclosing position, and only after a move. It is documented next to the feature rather than left to be discovered, and the showcase demo appends rather than reorders for exactly that reason.</p><p><strong>Internal:</strong> <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a> moves from 0.5.1 to 1.0.0 - the same code as 0.8.0 with a promise attached: thirty-three exported names are the public API, the binding spellings are settled, and a breaking change needs a major version. 961 tests there, up from 793. Components and slots are <em>not</em> part of this: Domma has its own web-component factory and never imports <code>registerComponent</code>, so that half of the package is tree-shaken out rather than bundled and dormant. 579 tests here, up from 577.</p>",
      "date": "2026-08-21"
    },
    {
      "year": "v0.39.2",
      "title": "Carousels With Words In",
      "description": "<p><strong>A carousel whose slides are text rather than pictures did not appear at all.</strong> One CSS rule; no API changes.</p><p><code>.carousel-slide-content</code> was unconditionally <code>position: absolute</code> - it is the caption overlay for a slide's background image. That works when the slide carries an <code>&lt;img&gt;</code>. When it does not, the caption was the slide's only child, so nothing was left in flow: the slide computed to 0px tall, the track and the carousel with it, and the component rendered as a gap. Arrows, indicators and transitions were all working the whole time, on an element with no height.</p><p>Slide content now sits in flow by default, and the overlay is stated as the enhancement for <code>.carousel-slide:has(&gt; img)</code>. In-flow content is also padded clear of the arrows and indicator dots.</p>",
      "date": "2026-08-21"
    },
    {
      "year": "v0.39.1",
      "title": "Options You Can Actually Tap",
      "description": "<p><strong>Forma's option groups get a real appearance, and the chooser stops squeezing itself onto phones.</strong> Two visual fixes and one spacing fix; no API changes.</p><p><strong>Radio and checkbox groups are styled.</strong> <code>type: 'radio'</code> and <code>type: 'checkbox-group'</code> rendered bare browser controls: the inputs carried no class at all, and there was no CSS anywhere for <code>.domma-radio-group</code> or <code>.domma-checkbox-group</code>. They now use <code>.form-check-input</code> - the same custom control <code>.form-check</code> has always had - and each option is a selectable tile: the whole label is the hit area, the checked state tints from <code>--dm-primary</code>, and the group wraps with <code>repeat(auto-fit, minmax(12rem, 1fr))</code>, dropping to one column below 480px.</p><p><strong>The card chooser steps down on narrow screens.</strong> <code>.picker-options</code> used <code>repeat(var(--picker-cols, 3), 1fr)</code> at every width, so a three-across grid stayed three-across on a 360px phone. The chooser now sets <code>--picker-cols-md</code> and <code>--picker-cols-sm</code> when it builds, both capped at the configured count, and steps down at 720px and 460px. A chooser configured with one or two columns is never widened.</p><p>Dialog footers also give the confirm button room to breathe.</p>",
      "date": "2026-08-21"
    },
    {
      "year": "v0.39.0",
      "title": "Six Ways In",
      "description": "<p><strong>Six new binding attributes and two new reactive powers</strong>, all of them things you previously had to write yourself. Nothing that already worked has changed.</p><p><strong><code>data-options</code></strong> fills a <code>&lt;select&gt;</code> from a collection, with <code>data-options-text</code>, <code>data-options-value</code> and <code>data-options-caption</code>. A <code>data-each</code> over <code>&lt;option&gt;</code> produces the same markup - what it does not produce is the <strong>selection</strong>, and that is the whole difficulty: rebuilding a select's options resets it, and the selection lives on the select rather than on any option, so a keyed list has nothing to preserve it with. Option values need not be strings: when the resolved value is an object or a number, the real value is kept alongside the option and <code>data-model</code> reads back <em>that</em>, not <code>\"[object Object]\"</code>. And a value the model asked for while no option carried it is remembered and applied by the rebuild that brings it - so attribute order does not matter, and neither does a collection that arrives from an <code>H.get()</code> long afterwards.</p><p><strong><code>data-focus</code></strong> is two-way between a value and focus - how a model puts the caret in the field it has just revealed without reaching for a DOM node, and how it knows where the user is without a listener. Unlike <code>data-model</code>, an expression it cannot write through is not fatal: focus still follows the value and only the write-back warns.</p><p><strong><code>data-bind-style-&lt;property&gt;</code> and <code>data-bind-style</code></strong> set one CSS property or a whole object of them. Two spellings because a binding expression has no object literal and will not grow one - parsing <code>{…}</code> safely is most of the way to the <code>eval</code> that Domma's Content Security Policy story depends on avoiding. Ownership follows the <code>class</code> rule, so a static <code>style=</code> attribute survives; a falsy value removes the property, and <code>0</code> is kept, because <code>opacity: 0</code> is a real value.</p><p><strong>Virtual bindings</strong> - <code>&lt;!-- dm if: x --&gt;</code> … <code>&lt;!-- /dm --&gt;</code>, plus <code>each</code> and <code>text</code>. A binding attribute needs an element to sit on, and sometimes there is none to spare: a run of <code>&lt;li&gt;</code>s, three <code>&lt;td&gt;</code>s in a row. Wrapping them in a <code>&lt;div&gt;</code> changes the layout, and inside a table it is not even valid HTML a browser will keep.</p><p><strong><code>M.observable(x).extend({…})</code></strong> layers behaviour onto an observable after it exists: <code>rateLimit</code>, <code>throttle</code> and <code>notify: 'always'</code>, with <code>M.registerExtender()</code> opening the same registry to you. <strong>The write is never delayed, only the announcement</strong> - worth stating plainly, because the obvious implementation of a throttle defers the write, and then every read between the write and the notification gives you a value that is already out of date.</p><p><strong><code>M.computed({read, write})</code></strong> is a computed you can write through, which is what lets <code>data-model=\"fahrenheit.value\"</code> bind a derived value. Assigning to one with no <code>write</code> now warns and names it, rather than storing into the read cache where the next recompute would silently drop it. <strong><code>M.observableArray()</code></strong> gains <code>indexOf()</code>, <code>replace()</code> and <code>destroy()</code> / <code>destroyAll()</code>, the last of which <em>marks</em> an item rather than removing it - for servers that delete on a flag in the payload.</p><p><strong>Internal:</strong> <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a> moves to 0.5.1 - 793 tests there, up from 679, and a step-by-step tutorial whose every listing is under test. All six bindings arrive with the pin, since the handler registry populates itself at import; what needed Domma code was the other two, reachable in the package and not through <code>M</code>. 577 tests here, up from 571.</p>",
      "date": "2026-08-09"
    },
    {
      "year": "v0.38.0",
      "title": "One Adapter",
      "description": "<p><strong>No new API.</strong> This release is the seam between Domma and the binding engine, made single - plus a limitation found by measuring rather than assuming, and now written down.</p><p><strong>One binding adapter, shared.</strong> The engine resolves every expression against <em>one</em> object and writes back through that same object. Getting either half wrong fails <strong>silently</strong>: a read-only snapshot swallows every <code>data-model</code> write - the control looks right, because what you see while typing is your own keystrokes - and an object carrying no functions resolves no <code>data-on-*</code> handler while every other binding on the same element works perfectly. Both shipped, and were fixed in v0.37.0; but the fix then existed <em>twice</em>, in two slightly different shapes. It is now once, in <code>src/binding-source.js</code>, with the failure modes documented where the code is rather than in two comment blocks that drift apart.</p><p><strong>Writing to a nested path does not notify the model.</strong> <code>data-model=\"profile.city\"</code>, where <code>profile</code> is a Model field holding an object, reads correctly and the write lands - but it mutates that object in place, so the field's observable never fires: the model is not told, and no other binding on that path updates. This was found while measuring whether <code>M.bind()</code>'s <code>parse</code> option could be retired. It cannot - <code>parse</code> is currently the only way to write a nested path and have the model notice, because it clones the field and returns a genuinely new value. The behaviour is now documented and pinned by a test, so that fixing it forces the documentation to move with it.</p><p><strong>Internal:</strong> <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a> moves to 0.4.2 - every published artefact now carries a version banner kept through minification, so a bundle is no longer anonymous and a stale <code>dist/</code> cannot be published as a fresh one. 571 tests, up from 570.</p>",
      "date": "2026-08-06"
    },
    {
      "year": "v0.37.0",
      "title": "The Binding Layer, Reachable",
      "description": "<p><strong><code>domma.min.js</code> has inlined the whole binding engine since v0.34.0, and almost none of it was reachable.</strong> Domma imported ten names from <code>domma-reactive</code> and re-exported nearly none, so the only route to a binding was a template inside <code>Domma.component()</code> - no way to bind server-rendered markup, and no way to register a custom binding. The evidence: across all 86 showcase pages there were <em>zero</em> live uses of <code>data-bind-*</code>, <code>data-model</code>, <code>data-each</code> or <code>data-if</code> outside a component, because outside a component there was no way to use them.</p><p><strong><code>M.applyBindings(data, root, options)</code></strong> activates every binding attribute under a root on markup that already exists - server-rendered, hand-written, whatever - in place, with no build step and no second source of truth for the markup. Pass a <strong>Model</strong> and it is converted to <code>model.tracked()</code>, the read-tracked write-through view, so a <code>data-model</code> write lands in the model with validation, change notification and persistence intact rather than in a snapshot. Handlers arrive as <code>options.methods</code>, since a Model holds data and not behaviour.</p><p><strong><code>M.registerBinding()</code> and <code>M.registerHelper()</code></strong> extend the vocabulary. The first is not a side door - all eight built-ins are registered through that same function, so anything a built-in can do a custom binding can do. The second exists because an expression deliberately cannot call a method on your data: <code>data-bind-text=\"upper(name)\"</code> is the supported way to shape a value in markup. Deliberately <em>not</em> exposed: <code>compile()</code> and <code>renderTemplate()</code>, because Domma already has <code>_.render</code> and the two diverge - publishing both would be a trap.</p><p><strong>Fixed - <code>data-on-*</code> and <code>data-model</code> in a component template.</strong> Methods live on the component context and were never merged into the template's data, so every event binding logged \"did not resolve to a function\" while every other binding on the same element worked. And <code>data-model</code> wrote back into a fresh plain snapshot, so the control looked right - what you see while typing is your own keystrokes - but the model never changed. Both were in the adapter between Domma and the engine; reads worked throughout, which is exactly why they survived.</p><p><strong>Internal:</strong> the showcase conventions sweep, 186 vanilla-JS call sites across 43 pages down to zero, plus <code>npm run validate:conventions</code> to keep it there. Three of the five are not straight swaps, which is why it was done by hand. <code>$(window)</code> is deliberately not flagged - it is an empty collection, so a mechanical sweep would have silently disabled every window listener on the site. <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a> moves to 0.4.1. 570 tests, up from 542.</p>",
      "date": "2026-08-06"
    },
    {
      "year": "v0.36.0",
      "title": "Thirteen Broken Pages",
      "description": "<p><strong>A browser-level test harness for the showcase, and the thirteen broken pages it found on its first run.</strong> There was no such coverage before: the unit suite exercises <code>src/</code> modules in isolation and cannot see a page that renders wrong, so the failure mode was invisible - no error, no failing test, just a page that was broken for every visitor.</p><p><strong><code>$('#el')[0]</code> works.</strong> <code>DommaCollection</code> exposed <code>.elements</code> and <code>.get(i)</code> but no numeric index properties, so the most-typed thing in jQuery returned <code>undefined</code> - silently, until something dereferenced it. The DOM showcase documents <code>$('.items')[0]</code> as the same as <code>get(0)</code>, and 65 call sites across the repository use the form. The collection is now array-like, so <code>Array.from()</code> works; it is still not iterable.</p><p><strong><code>Domma.init()</code> has never existed</strong>, and calling it threw. On the CSS-utilities showcase it sat at the top of the page's only script, so everything below it died - that page's reference table rendered nothing at all. Four separate documents told you to call it. Domma self-initialises when the bundle loads; the only startup call a page needs is <code>Domma.icons.scan()</code>.</p><p><strong>Also fixed:</strong> Pillbox was handed a <code>&lt;select&gt;</code> and a <code>&lt;div&gt;</code> on two showcases; the timeline showcase called its own deprecated alias six times on a page headed \"Component Deprecated\"; sidebar push mode targeted a <code>.main-content</code> that did not exist; and <code>theme: 'light'</code> is not a theme name. <strong>Added:</strong> four icons that pages were already using and that silently rendered nothing - <code>alert-triangle</code>, <code>sidebar</code>, <code>window</code> and <code>gem</code>.</p><p><strong>Internal:</strong> <code>npm run validate:showcase</code> loads all 85 pages into jsdom against the built bundle and asserts each rendered and logged nothing. The baseline is empty - every page passes in strict mode, so any finding is a failure. Built as the prerequisite for the showcase conventions sweep: 191 violations across 54 pages, which is not something to rewrite without a harness.</p>",
      "date": "2026-08-05"
    },
    {
      "year": "v0.35.0",
      "title": "A List's Rows Can Act on the List",
      "description": "<p><strong>One new Domma API: <code>M.computed().value</code></strong> - the same read as <code>get()</code>. <code>M.observable()</code> has always been read through <code>.value</code>, while <code>M.computed()</code> offered only <code>get()</code>, so the two halves of the same idea disagreed about how you read them. It also made a computed unreadable from a template expression, where a method cannot be called: <code>{{total.get()}}</code> does not parse, and never will, because a call inside a render is a side effect.</p><p><strong><code>M.observableArray().remove()</code> now takes a value or a test.</strong> <code>rows.remove(row)</code> matches by identity as before; <code>rows.remove(r =&gt; r.id &gt; 2)</code> removes everything the test accepts. A function used to be compared against each item by identity, never matched, and removed nothing without a word.</p><p><strong>Fixed - <code>&amp;&amp;</code> broke a binding inside a keyed block.</strong> A keyed block's body is captured by serialising DOM back to HTML, which escapes every <code>&amp;</code> it writes, so <code>data-bind-class=\"done &amp;&amp; 'struck'\"</code> - the documented idiom - came back out as an entity and failed to parse. Attribute values read as expressions are now decoded in a single pass, so <code>&amp;amp;lt;</code> cannot double-decode.</p><p><strong>Fixed - a row could not reach the list that owns it.</strong> Inside a list <code>$data</code> is the item, so <code>$parent.remove($data)</code> was the only way to name the parent, and it did not parse. <code>data-on-*</code> may now call a method on your data. The restriction is scoped rather than lifted: only the event binding may do it, and the evaluator still refuses to perform a method call, so <code>{{ }}</code>, <code>data-if</code> and <code>data-bind-*</code> remain reads with no side effects.</p><p><strong>Internal:</strong> <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a> moves to 0.4.0 - keyed reconciliation, instance lifecycle and <code>applyBindings()</code>, plus the fixes above. 676 tests, every new guarantee mutation-tested.</p>",
      "date": "2026-08-05"
    },
    {
      "year": "v0.34.0",
      "title": "CSP-Safe Expressions & a Binding Registry",
      "description": "<p><strong>No Domma API changes.</strong> Everything here arrives through the pinned <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a> 0.3.0, which Rollup still inlines - <code>domma.min.js</code> remains a single self-contained file.</p><p><strong>A CSP-safe expression evaluator.</strong> Tokeniser, Pratt parser, AST walker and helper registry, supporting property paths, indexing, comparison, logical operators, ternaries, arithmetic and literals. <strong>No <code>eval</code> and no <code>Function</code> constructor anywhere</strong> - in the source or in any built bundle. Knockout evaluates bindings with the <code>Function</code> constructor, which fails under <code>script-src 'self'</code> without <code>unsafe-eval</code>; Domma's expressions work under a strict CSP. Property access through <code>__proto__</code>, <code>constructor</code> or <code>prototype</code> is refused in every form, including a computed key whose value is only <code>'__proto__'</code> at runtime.</p><p><strong>A binding registry</strong> with <code>registerBinding()</code> and four behaviour bindings: <code>data-on-*</code> for events, <code>data-bind-*</code> for properties and attributes, <code>data-model</code> for two-way binding, and <code>data-if</code>, which removes the element rather than hiding it. The four existing mustache kinds now register through the same public API, so a custom binding is not a second-class citizen. There is deliberately no <code>data-bind-html</code>.</p><p><strong>Fixed:</strong> seven rules painted with variables defined nowhere, so those backgrounds were simply absent. <strong>Added:</strong> two repository validators (<code>npm run validate</code>) that catch dead CSS classes and unreadable theme contrast - both failure modes that produce no error and no failing test, just markup that renders wrong.</p>",
      "date": "2026-08-05"
    },
    {
      "year": "v0.33.1",
      "title": "Millisecond Waits",
      "description": "<p><strong>A <code>'300ms'</code> wait ran for five minutes.</strong> <code>parseWait()</code> in the scribe effect tested <code>endsWith('s')</code> before <code>endsWith('ms')</code> - and because <code>'ms'</code> also ends with <code>'s'</code>, the millisecond branch was unreachable. Every millisecond-suffixed wait was multiplied by 1000, so a scribe sequence with a <code>{wait: '300ms'}</code> step appeared to stop after the first render.</p><p>Seconds (<code>'2s'</code>) and raw numbers were unaffected, which is why it went unnoticed - the workaround was to pass milliseconds as a number. This was reported in the v0.25.2 notes as queued for a future patch and has been present ever since.</p><p><strong>Internal:</strong> the helper was closure-local inside <code>scribe()</code> and therefore impossible to test. It is now a module-scope export, matching <code>resolvePalette</code>, pinned by tests that fail if the branch ordering is restored. The public <code>Domma.effects</code> surface is unchanged.</p>",
      "date": "2026-08-05"
    },
    {
      "year": "v0.33.0",
      "title": "Working Scaffolds & a Real Reactivity Showcase",
      "description": "<p><strong>Fixed - every scaffolded project rendered unstyled.</strong> The kickstart templates were written in Bootstrap class names while Domma ships Tailwind-style utilities, so <strong>143 class usages across 14 files resolved to nothing</strong>. Anyone who ran <code>npx domma init</code> got a project that looked broken on first load. Two components were dead rather than merely unstyled: the contact FAQ and docs pages used Bootstrap's collapse markup, and Bootstrap JS is never loaded - while <code>contact.js</code> and <code>docs.js</code> already called <code>Domma.elements.accordion()</code> and <code>.tabs()</code> against markup they could not recognise. Both work now.</p><p><strong>The Reactivity showcase is a showcase, not a reference.</strong> It had six sections, every one named after a mechanism and opening by explaining it - no statement of the problem, no benefit, no guidance on when not to use it. Now twelve sections named for outcomes, demonstration before explanation, opening with the code you stop writing, and a <em>When not to reach for it</em> section that shows the traps running live. Prose grew from 506 to 4,118 words; all 26 interactive demos preserved. See the <a href=\"../showcase/models/reactivity.html\">Reactivity showcase</a>.</p><p><strong>New:</strong> Try-it panes - editable code with live output on any showcase page - plus the <code>.list-none</code> and <code>.no-underline</code> utilities.</p><p><strong>Internal:</strong> the template compiler moved into <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a> 0.2.0, completing the extraction milestone that had shipped half-done.</p>",
      "date": "2026-08-05"
    },
    {
      "year": "v0.32.0",
      "title": "Observables, Badges & Readable Buttons",
      "description": "<p><strong>New:</strong> <code>M.observable()</code> and <code>M.observableArray()</code> - the reactive primitives beneath Models are now reachable from Domma. Use <code>M.create()</code> when you want a schema, validation and persistence; use an observable when you want one tracked value and nothing else. Both are the same functions published standalone as <a href=\"https://www.npmjs.com/package/domma-reactive\">domma-reactive</a>.</p><p><strong>Fixed - components stopped updating.</strong> <code>onUpdated()</code> never fired for components whose templates contain no <code>{{ }}</code> bindings, so anything rendering its content imperatively rendered once and then went silent - the model updated and persisted, but the DOM was never told and nothing was thrown. This affected the todo, notes, contacts and markdown examples. Introduced in v0.30.0. Writes made from <code>onUpdated</code> must now converge on a value that compares equal, or the watcher re-triggers indefinitely.</p><p><strong>Fixed - unreadable controls.</strong> Outline buttons used a transparent background with a <code>color-mix()</code> blend, so their contrast depended on whatever sat behind them and the label could land on its own background. Eighteen showcase rules set a background from a variable no theme overrides, with no text colour at all. <code>.table-responsive</code> was used in fourteen places and defined nowhere. The Reactivity showcase used <code>.form-control</code> and <code>.col-md-*</code>, neither of which exists in Domma.</p><p><strong>Known issue:</strong> <code>onUpdated</code> still does not fire for fields absent from <code>data()</code> - declare every field you intend to change.</p>",
      "date": "2026-08-04"
    },
    {
      "year": "v0.25.2",
      "title": "Homepage - Domma CMS Point-of-Sale Band",
      "description": "<p><strong>Site Update:</strong> A new full-bleed point-of-sale section on the Domma JS homepage, sitting between the feature grid and the Examples section, pitching <a href=\"https://cms.dommajs.org/\"><strong>Domma CMS</strong></a> without leaving the page. The section doubles as a working showcase of the effects library - six different Domma effects fire in choreographed sequence as it scrolls into view.</p><p><strong>What's in the band:</strong></p><ul><li><strong>Looping headline</strong> - the <code>Domma.effects.scribe</code> typewriter cycles four pitches: <em>'Want Domma JS power in a CMS?'</em>, <em>'Meet Domma CMS.'</em>, <em>'Markdown-first. Zero build step.'</em>, and <em>'Self-hosted. Open. Ridiculously fast.'</em> - typing, holding, erasing and retyping in an infinite loop with a blinking cursor.</li><li><strong>Stat tiles with stagger-in + counter cascade</strong> - four key numbers (27 shortcodes, 28 themes, 6 free plugins, 0 build steps) zoom-stagger into view, then <code>Domma.effects.counter</code> ticks each value up. The 'Build steps' tile counts <em>down</em> from 60 → 0 for dramatic effect.</li><li><strong>Animated CTAs</strong> - primary <em>Get Started Free</em> sits clean and bold; secondary <em>See Features</em> uses <code>Domma.effects.pulse</code> for a gentle attention pull.</li><li><strong>Pastel ticker-tape burst</strong> - <code>Domma.effects.tickerTape</code> fires a one-shot confetti burst (40 strips, pastel palette) scoped to the band when it reveals.</li><li><strong>Float-blob ambient backdrop</strong> - both the band and the splash hero now use <code>.bg-ambient-float-blobs</code> for slowly-drifting indigo/blue radial gradients.</li></ul><p><strong>Effects choreographed:</strong> <code>reveal</code> → <code>scribe</code> (looping) → <code>counter</code> (cascade) → <code>tickerTape</code> (burst) → <code>pulse</code> - all triggered by a single <code>IntersectionObserver</code> with a <code>requestAnimationFrame</code> fallback for elements already in the viewport on initial load.</p><p><strong>Accessibility note:</strong> Because this section <em>is</em> the pitch, its effects override <code>prefers-reduced-motion</code> via <code>respectMotionPreference: false</code>. The rest of the homepage still respects the user's motion toggle.</p><p><strong>Known issue surfaced:</strong> The work uncovered an ordering bug in <code>parseWait</code> inside <code>src/effects.js</code> - <code>endsWith('s')</code> is checked before <code>endsWith('ms')</code>, so any millisecond-suffixed wait string is multiplied by 1000 (a <code>'300ms'</code> wait becomes 5 minutes). Worked around in the homepage by passing numeric milliseconds; fix queued for a future patch release.</p>",
      "date": "2026-05-20"
    },
    {
      "year": "v0.25.1",
      "title": "Carousel - Crossfade Mode, Custom Easing & Demo Gradients",
      "description": "<p><strong>Enhancement Release:</strong> The <strong>Carousel</strong> element gains a third transition mode and a fully configurable easing curve, with a small library of decorative demo gradients added to the showcase styling layer.</p><p><strong>New transition mode - <code>animation: 'crossfade'</code>:</strong> Outgoing and incoming slides transition opacity <em>at the same time</em>, briefly overlapping mid-transition. Cinematic and soft, ideal for hero imagery and feature reels where the existing <code>'fade'</code> (clean swap) reads too abruptly. The outgoing slide remains <code>position: relative</code> for the duration of the overlap so the container holds its height - no mid-transition layout reflow. After the transition completes the incoming slide is promoted to the height-keeper role on a single <code>setTimeout</code>, with destroy-time cleanup.</p><p><strong>New option - <code>animationEasing</code>:</strong> Any CSS timing function is now accepted (<code>'ease'</code>, <code>'linear'</code>, <code>'ease-in-out'</code>, <code>'cubic-bezier(...)'</code>, etc.) and is threaded into both the fade/crossfade opacity transition and the slide-mode track <code>transform</code> transition. Default remains <code>'ease'</code> - fully backwards-compatible.</p><p><strong>Showcase rebuild:</strong> A dedicated <strong>Transitions</strong> section on the Carousel showcase page now demonstrates fade, crossfade, and a bouncy <code>cubic-bezier</code> easing example with autoplay + hover-pause, so the effects play themselves the moment the section comes into view.</p><p><strong>Demo Gradient Palette:</strong> Nine reusable decorative gradients added to <code>domma-showcase.css</code> under the <code>.demo-gradient-*</code> namespace, organised by mood - <strong>aurora</strong> (vivid high-saturation), <strong>dusk</strong> (cinematic atmospheric), <strong>pop</strong> (bright energetic). Showcase-internal, not part of the framework's public CSS API.</p><p><strong>Documentation:</strong> PHPStorm typings (<code>CarouselOptions.animation</code> widened to <code>'slide' | 'fade' | 'crossfade'</code>, <code>animationEasing?: string</code> added); <code>src/CLAUDE.md</code> module reference updated with transition mode descriptions; <code>ELEMENTS.md</code> gains a Transition Modes table and a Demo Gradient Palette reference.</p><p><strong>Quick start:</strong></p><p><code>Domma.elements.carousel('#hero', { animation: 'crossfade', animationDuration: 1400, animationEasing: 'ease-in-out' });</code></p>",
      "date": "2026-05-16"
    },
    {
      "year": "v0.25.0",
      "title": "Chooser - Visual Option-Picker for Forms",
      "description": "<p><strong>Feature Release:</strong> A new <strong>Chooser</strong> form input - Domma's 25th UI component - replacing the visual weight of native radio buttons and checkboxes when a choice deserves richer presentation. Cards or chips, single or multi-select, all four combinations driven by one unified API.</p><p><strong>The four-way matrix:</strong></p><ul><li><strong>Card · single-select</strong> - Pricing-plan style; rich tiles with icon, label, description, and corner badge</li><li><strong>Card · multi-select</strong> - Feature picker with checkbox semantics inside the same card layout</li><li><strong>Chip · single-select</strong> - Compact pill row for terse choices</li><li><strong>Chip · multi-select</strong> - Tag picker with full keyboard support</li></ul><p><strong>Per-option metadata:</strong></p><ul><li><code>icon</code> - any Domma icon name; renders inline</li><li><code>description</code> - sub-text under the label (card + comfortable density only)</li><li><code>tooltip</code> - hover hint via <code>data-tooltip</code></li><li><code>badge: { text, type }</code> - corner badge in any of five semantic colours, reusing the existing <code>.badge-*</code> classes</li><li><code>recommended: true</code> - success-coloured border ring for the highlighted option</li><li><code>disabled: true</code> - muted, non-interactive, skipped by keyboard navigation</li></ul><p><strong>Visual polish - six top-level options:</strong></p><ul><li><code>accent</code> - selected highlight colour; semantic name (<code>primary</code>, <code>success</code>, <code>info</code>, <code>warning</code>, <code>danger</code>) or any CSS colour string</li><li><code>accentStyle</code> - five distinct treatments: <code>border</code> (default), <code>solid</code> (filled tile), <code>glow</code> (transparent border + glowing ring), <code>overlay</code> (heavier translucent fill), <code>underline</code> (thick coloured bottom border)</li><li><code>glow</code> + <code>glowColour</code> - soft outer glow on the selected option, optional colour override</li><li><code>shadow</code> - <code>none</code> / <code>sm</code> / <code>md</code> / <code>lg</code> / <code>xl</code> applied to every option</li><li><code>shadowColour</code> - optional shadow tint</li></ul><p><strong>Theme awareness:</strong> Semantic colours map to existing Domma CSS variables and retint live across all 26 themes × light/dark variants. Custom hex/rgb values become inline CSS custom properties on the chooser root, so they remain consistent across the four matrix combinations.</p><p><strong>Form integration:</strong> Use <code>type: 'chooser'</code> in any <code>F.create()</code> blueprint and the form pipeline handles validation, model binding, and submission automatically. Required validation treats an empty array as empty for multi-select. Hidden native <code>&lt;input type=\"radio|checkbox\"&gt;</code> elements are emitted when <code>name</code> is set so the value is captured by <code>FormData</code> even with JS disabled.</p><p><strong>Accessibility:</strong> <code>role=\"radiogroup\"</code> for single-select with arrow-key navigation; <code>role=\"group\"</code> for multi-select with Tab + Space; <code>aria-checked</code> reflects state; disabled options skipped by keyboard; respects <code>prefers-reduced-motion</code>.</p><p><strong>CMS Page Editor integration:</strong> The page-roller's form-section editor gains a draggable <strong>Add Chooser</strong> palette tile. Clicking it opens a slideover with a structured editor - field-level inputs, chooser-level toggles, full options editor with add/remove/reorder, six visual-option fields (accent, accent style, glow, glow colour, shadow, shadow colour), and a <strong>live preview pane</strong> that updates as you type. Save writes back into the <code>formFields</code> JSON textarea (still the canonical store). Click any rendered chooser tile in the canvas to re-open the slideover pre-populated for editing.</p><p><strong>Showcase:</strong> Comprehensive page at <code>/showcase/elements/chooser/</code> covering all four matrix combinations, density and columns demos, every per-option flag (with all five badge types), accent colour and accent style galleries, glow and shadow variants, in-form integration, standalone usage, and a long-form tutorial with three real-world patterns (pricing plan, tag picker, theme picker) plus a gotchas section.</p><p><strong>Documentation:</strong> Full API reference in <code>docs/API.md</code>; blueprint shape and validation in <code>docs/Blueprints.md</code>; feature section with theme-awareness notes in <code>docs/DommaDocumentation.md</code>; PHPStorm typings include <code>ChooserOptions</code>, <code>ChooserOption</code>, <code>ChooserBadge</code>, <code>ChooserInstance</code>, and <code>ChooserSemanticColour</code>.</p><p><strong>Quick start:</strong></p><p><code>Domma.elements.chooser('#host', { variant: 'card', options: [&hellip;] });</code> - standalone.</p><p><code>F.create({ plan: { type: 'chooser', variant: 'card', options: [&hellip;] } }).renderTo('#form');</code> - inside a blueprint-driven form.</p>",
      "date": "2026-05-09"
    },
    {
      "year": "v0.24.0",
      "title": "Ticker Tape - Confetti-Parade Effect",
      "description": "<p><strong>Feature Release:</strong> A new canvas-based <strong>ticker tape</strong> effect joins the <code>Domma.effects</code> family - colourful rectangular strips drop from the top of a container (or the entire viewport), sway, rotate, and gently fade before reaching the bottom. Reminiscent of a ticker-tape parade, perfect for celebration moments.</p><p><strong>What it does:</strong></p><ul><li><strong>Two modes</strong> - Pass <code>null</code> for a fixed full-page overlay, or a selector for a container-scoped effect; the canvas reflows automatically via <code>ResizeObserver</code></li><li><strong>Theme-aware palettes</strong> - The default <code>palette: 'theme'</code> reads <code>--dm-primary</code>, <code>--dm-success</code>, <code>--dm-warning</code>, <code>--dm-danger</code>, and <code>--dm-info</code> from the active Domma theme, so the same call adapts to whichever theme is loaded</li><li><strong>10 named palettes</strong> - <code>rainbow</code>, <code>festive</code>, <code>gold</code>, <code>silver</code>, <code>pastel</code>, <code>mono</code>, <code>sunset</code>, <code>ocean</code>, <code>forest</code>, and <code>bridal</code>; supply your own array of CSS colour strings for branded confetti</li><li><strong>Continuous or one-shot</strong> - The default mode maintains a steady stream up to <code>density</code> strips on screen; <code>burst: true</code> drops a single batch (<code>burstCount</code> strips) and lets the effect end naturally - ideal for form-success or achievement-unlocked moments</li><li><strong>Tunable physics</strong> - <code>speed</code>, <code>sway</code>, <code>rotationSpeed</code>, strip width / height ranges, and <code>fadeStart</code> (where in the fall fade begins) all adjustable</li><li><strong>Standard control surface</strong> - Returns the same <code>{ pause, resume, stop, restart, destroy, isRunning, isPaused }</code> object as every other <code>Domma.effects</code> method; honours <code>prefers-reduced-motion</code> by default</li><li><strong>Showcase</strong> - Dedicated page at <code>/showcase/effects/ticker-tape.html</code> with five demos: theme-aware container, palette picker (live swap), physics customisation sliders, one-shot burst, and full-page overlay</li><li><strong>Documentation</strong> - Full API reference in <code>docs/API.md</code>; PHPStorm typings updated; new <code>party-popper</code> icon added to the seasonal set</li></ul><p><strong>Quick start:</strong></p><p><code>Domma.effects.tickerTape(null);</code> - full-page parade in the active theme's colours.</p><p><code>Domma.effects.tickerTape('#stage', { burst: true, palette: 'gold' });</code> - celebration burst on demand.</p>",
      "date": "2026-04-22"
    },
    {
      "year": "v0.23.0",
      "title": "Signature - Canvas Handwriting Pad",
      "description": "<p><strong>Feature Release:</strong> A full-featured <strong>Signature</strong> element - Domma's 24th UI component - providing a canvas-based handwriting pad for capturing signatures in forms, contracts, and consent flows.</p><p><strong>Input &amp; Drawing:</strong></p><ul><li><strong>Pointer Events API</strong> - Unified mouse, touch, and stylus input; pressure-sensitive line width on stylus devices with graceful fallback for mouse and touch</li><li><strong>Smooth Bézier curves</strong> - Midpoint quadratic Bézier algorithm prevents jagged strokes; variable lineWidth per segment from pressure data</li><li><strong>Normalised co-ordinates</strong> - Stroke points stored as 0-1 fractions so a <code>ResizeObserver</code> can reflow all strokes correctly when the container is resized</li></ul><p><strong>Undo / Redo &amp; Export:</strong></p><ul><li><strong>Full stroke history</strong> - <code>sig.undo()</code> / <code>sig.redo()</code> operate stroke-by-stroke; <code>sig.clear()</code> is itself undoable; <code>sig.clear(true)</code> for a silent, irreversible wipe</li><li><strong>PNG export</strong> - <code>sig.toBase64('png')</code> returns a canvas data URL</li><li><strong>SVG export</strong> - <code>sig.toBase64('svg')</code> rebuilds paths from stored strokes using the same Bézier algorithm as SVG <code>M</code>, <code>Q</code>, <code>L</code> path commands; output is visually identical to the canvas render</li></ul><p><strong>Toolbar &amp; Type Fallback:</strong></p><ul><li><strong>Configurable toolbar</strong> - Colour swatches, pen-width dot buttons, undo, redo, and clear; driven by <code>colours</code> and <code>widths</code> options; hideable with <code>toolbar: false</code></li><li><strong>Type fallback</strong> - <code>typeFallback: true</code> adds a Draw / Type toggle in the toolbar; Type mode renders cursive-font text to the canvas via <code>fillText()</code> so <code>toBase64()</code> still returns an image; <code>isEmpty()</code> correctly reflects typed content</li></ul><p><strong>Form Integration &amp; Config:</strong></p><ul><li><strong>Hidden input</strong> - Auto-populated with the base64 value after each stroke; the component works inside any <code>&lt;form&gt;</code> for native submission</li><li><strong>Blueprint support</strong> - Use <code>type: 'signature'</code> in an <code>F.create()</code> blueprint; the form builder initialises the pad and syncs the model automatically</li><li><strong>Config Engine</strong> - <code>signature</code> added to the component map for declarative <code>$.setup()</code> initialisation</li></ul><p><strong>Accessibility:</strong> <code>role=\"application\"</code> on the container, <code>aria-pressed</code> on all toolbar buttons, <code>aria-live=\"polite\"</code> status bar, <code>tabindex=\"0\"</code> on the canvas, and <code>respectMotionPreference</code> support.</p><p><strong>Showcase:</strong> Dedicated page at <code>/showcase/elements/signature/</code> with 13 sections including a live demo, pen options, export formats, undo/redo, type fallback, disabled state, form integration, config engine, contract-signing tutorial, and full API reference.</p>",
      "date": "2026-03-23"
    },
    {
      "year": "v0.22.0",
      "title": "List Group - Numbered, Horizontal & Selectable Lists",
      "description": "<p><strong>Feature Release:</strong> A full-featured <strong>List Group</strong> element - Domma's 23rd UI component - providing polished, consistent list styling for settings panels, navigation menus, notification feeds, and content lists.</p><p><strong>CSS Classes:</strong></p><ul><li><strong>Base</strong> - <code>.list-group</code> container with <code>.list-group-item</code> items; active, disabled, and <code>.list-group-item-action</code> states with hover/focus styling; sub-element helper classes for rich layouts: <code>.list-group-item-icon</code>, <code>.list-group-item-content</code>, <code>.list-group-item-heading</code>, <code>.list-group-item-text</code>, <code>.list-group-item-trailing</code></li><li><strong>Variants</strong> - <code>.list-group-flush</code> strips outer borders and radius for embedding inside cards; <code>.list-group-sm</code> and <code>.list-group-lg</code> adjust padding and font size; 6 semantic colour variants (<code>-primary</code>, <code>-secondary</code>, <code>-success</code>, <code>-danger</code>, <code>-warning</code>, <code>-info</code>) apply a left-border accent with a tinted background</li><li><strong>Numbered</strong> - <code>.list-group-numbered</code> on an <code>&lt;ol&gt;</code> element prepends auto-incrementing counters via CSS - no JavaScript needed</li><li><strong>Horizontal</strong> - <code>.list-group-horizontal</code> arranges items side-by-side; responsive variants <code>-sm</code> (576 px), <code>-md</code> (768 px), <code>-lg</code> (992 px), <code>-xl</code> (1200 px) switch to horizontal only above the given breakpoint</li></ul><p><strong>JavaScript API:</strong> <code>E.listGroup(selector, options)</code> enables selectable lists with single or multi-select, roving tabindex keyboard navigation (Arrow keys, Home/End, Enter/Space), and full ARIA semantics (<code>role=\"listbox\"</code>, <code>aria-selected</code>, <code>aria-disabled</code>, <code>aria-multiselectable</code>). Public methods: <code>select(i)</code>, <code>deselect(i)</code>, <code>toggle(i)</code>, <code>selectAll()</code>, <code>deselectAll()</code>, <code>getSelected()</code>, <code>isSelected(i)</code>, <code>enable(i)</code>, <code>disable(i)</code>, <code>getItems()</code>, <code>refresh()</code>, <code>destroy()</code>. Callbacks: <code>onChange</code>, <code>onSelect</code>, <code>onDeselect</code>.</p><p><strong>Config Engine:</strong> <code>listGroup</code> added to the supported component map for declarative <code>$.setup()</code> initialisation.</p><p><strong>Showcase:</strong> Dedicated page at <code>/showcase/elements/list-group/</code> with demos covering all CSS variants, numbered lists, horizontal layouts, interactive JS API (selection, multi-select, keyboard navigation), a settings-panel tutorial, API reference tables, and a CSS customisation guide. Linked from the Elements overview page and the Elements dropdown navigation.</p>",
      "date": "2026-03-19"
    },
    {
      "year": "v0.21.0",
      "title": "Number Badge - Notification Counters & Presence Dots",
      "description": "<p><strong>Feature Release:</strong> A new <strong>Number Badge</strong> component extends Domma's badge system with positioned notification counters and presence indicators, available as CSS classes, a JavaScript API, and a <code>&lt;domma-number-badge&gt;</code> web component.</p><p><strong>CSS Classes:</strong></p><ul><li><strong><code>.badge-number</code></strong> - Standalone circular badge for single digits, expanding to a pill for multi-digit numbers via <code>min-width</code></li><li><strong><code>.badge-dot</code></strong> - Small 8px circular presence indicator (no text)</li><li><strong><code>.badge-counter-wrapper</code> / <code>.badge-counter</code></strong> - Wrapper + absolute-positioned counter that overlaps the top-right corner of any element with a border ring</li><li><strong>8 colour variants</strong> - <code>-primary</code>, <code>-secondary</code>, <code>-success</code>, <code>-danger</code>, <code>-warning</code>, <code>-info</code>, <code>-light</code>, <code>-dark</code> for all three class families</li><li><strong><code>.badge-pulse</code></strong> - Reuses the existing pulse keyframe animation for urgent counters</li></ul><p><strong>JavaScript API:</strong> <code>E.numberBadge(selector, options)</code> wraps the target element in a <code>&lt;domma-number-badge&gt;</code> web component and returns a chainable instance with <code>increment()</code>, <code>decrement()</code>, <code>setCount()</code>, <code>setVariant()</code>, <code>setDot()</code>, <code>setPulse()</code>, and <code>remove()</code>. The badge auto-hides when count reaches zero in non-dot mode, and adds <code>aria-live=\"polite\"</code> and <code>role=\"status\"</code> for screen reader announcements.</p><p><strong>Web Component <code>&lt;domma-number-badge&gt;</code>:</strong> Shadow DOM custom element with a <code>&lt;slot&gt;</code> for child content. Observed attributes: <code>count</code>, <code>variant</code>, <code>dot</code>, <code>pulse</code>, <code>border-color</code>. Full programmatic API mirrors the JS wrapper.</p><p><strong>Config Engine:</strong> <code>numberBadge</code> added to the supported component map for declarative <code>$.setup()</code> configuration.</p><p><strong>Showcase:</strong> Dedicated page at <code>/showcase/elements/number-badge/</code> with CSS-only variant grids, interactive JS API demo (increment / decrement / dot / pulse / variant controls), web component examples, real-world patterns (mock navbar, inbox unread counts, online user presence list), API reference tables, and CSS customisation guide.</p>",
      "date": "2026-03-18"
    },
    {
      "year": "v0.20.2",
      "title": "Opacity Scale & Translucent Utilities",
      "description": "<p><strong>Enhancement Release:</strong> A complete opacity scale and a set of semantic translucency utilities have been added to <code>domma.css</code>, replacing the four sparse <code>.opacity-*</code> classes that previously existed.</p><p><strong>Full Opacity Scale:</strong> 13 utility classes cover the complete range in steps of 10, with additional quarter-stop values - <code>.opacity-0</code>, <code>.opacity-10</code>, <code>.opacity-20</code>, <code>.opacity-25</code>, <code>.opacity-30</code>, <code>.opacity-40</code>, <code>.opacity-50</code>, <code>.opacity-60</code>, <code>.opacity-70</code>, <code>.opacity-75</code>, <code>.opacity-80</code>, <code>.opacity-90</code>, <code>.opacity-100</code>.</p><p><strong>Semantic Translucent Utilities:</strong></p><ul><li><code>.translucent-light</code> - Subtle transparency (<code>opacity: 0.85</code>); ideal for watermarks and overlays</li><li><code>.translucent</code> - Standard transparency (<code>opacity: 0.70</code>); for modal panels and frosted overlays</li><li><code>.translucent-heavy</code> - Strong transparency (<code>opacity: 0.50</code>); for ghost elements</li></ul><p>All three classes include <code>transition: opacity var(--dm-transition-normal)</code> so state changes animate without extra CSS.</p><p><strong>Frosted Glass:</strong> <code>.translucent-glass</code> applies a semi-transparent white background (<code>rgba(255,255,255,0.75)</code>) combined with <code>backdrop-filter: blur(8px)</code> and its <code>-webkit-</code> prefixed counterpart for broad browser coverage. Falls back gracefully to just the transparent background where <code>backdrop-filter</code> is unsupported.</p><p><strong>Hover Variants:</strong> <code>.translucent-hover</code>, <code>.translucent-light-hover</code>, and <code>.translucent-heavy-hover</code> apply their respective opacity levels only on <code>:hover</code>, with the built-in transition. Useful for image grids and icon buttons.</p><p><strong>CSS Custom Properties:</strong> All translucency values are driven by five new <code>:root</code> variables - <code>--dm-translucent-light</code>, <code>--dm-translucent</code>, <code>--dm-translucent-heavy</code>, <code>--dm-translucent-glass-blur</code>, and <code>--dm-translucent-glass-opacity</code> - making them straightforward to override per-theme.</p><p><strong>Showcase:</strong> New dedicated page at <code>/showcase/css-utilities/</code> with a visual opacity swatch grid, an interactive range-slider demo, translucent card examples, hover-variant demo boxes, a frosted-glass live demo over a coloured gradient, and a full reference table.</p>",
      "date": "2026-03-17"
    },
    {
      "year": "v0.19.7",
      "title": "Card Accent Variant - First-Class `.card-accent` Classes",
      "description": "<p><strong>Enhancement Release:</strong> The left-border card accent pattern has been promoted from an inline style workaround to a set of first-class CSS classes, covering the five standard semantic colours.</p><p><strong>New CSS Classes:</strong></p><ul><li><code>.card-accent</code> - Primary colour left border (4 px), uses <code>--dm-primary</code></li><li><code>.card-accent-success</code> - Success green left border, uses <code>--dm-success</code></li><li><code>.card-accent-danger</code> - Danger red left border, uses <code>--dm-danger</code></li><li><code>.card-accent-warning</code> - Warning amber left border, uses <code>--dm-warning</code></li><li><code>.card-accent-info</code> - Info sky left border, uses <code>--dm-info</code></li></ul><p><strong>Theme Variable Sweep:</strong> <code>--dm-card-accent</code> added to all 26 theme files as an alias for the primary colour, enabling per-theme accent customisation and supporting the JS <code>color: 'accent'</code> API option.</p><p><strong>Showcase:</strong> Dedicated <em>Card Accent</em> section added to the Elements showcase; inline <code>border-left</code> style on the elements index replaced with the new class.</p><p><strong>Repo Hygiene:</strong> Removed stale backup files (<code>src/css/domma.css.original</code>, <code>public/showcase/css/domma.css.original</code>), stale TODO (<code>public/showcase/elements/CSS_CUSTOMISATION_SECTIONS_TODO.md</code>), empty directories (<code>public/showcase/js/</code>, <code>public/showcase/examples/</code>), and added <code>.superpowers/</code>, <code>firebase-debug.log</code>, and <code>release.json</code> to <code>.gitignore</code>.</p>",
      "date": "2026-03-17"
    },
    {
      "year": "v0.19.6",
      "title": "Christmas - \"Frosty Morning\" & \"Christmas Eve\" Theme Overhaul",
      "description": "<p><strong>Enhancement Release:</strong> Full palette rewrite of both Christmas theme variants - moving from generic white/slate backgrounds to genuinely festive palettes.</p><p><strong>Christmas Theme Overhaul:</strong></p><ul><li><strong>christmas-light (\"Frosty Morning\")</strong> - Ice-blue tinted backgrounds (<code>#F5F8FC</code>) evoking frost on windows, deep pine-green text (<code>#1A2A1E</code>), classic Christmas Red primary (<code>#C41E3A</code>), rich Holly Green secondary (<code>#2D6B3F</code>), and frosty blue-tinted shadows</li><li><strong>christmas-dark (\"Christmas Eve\")</strong> - Deep forest green backgrounds (<code>#0A1A10</code>) replacing generic slate, warm candlelight cream text (<code>#F0ECE4</code>), bright red primary (<code>#E85565</code>) for dark-mode readability, bright holly secondary (<code>#5CB875</code>), and warm firelight-tinted shadows</li></ul><p><strong>Decorative CSS:</strong> Snow-dust radial-gradient dot pattern, red-gold-green gradient card top-borders via <code>border-image</code>, candy-stripe progress bars, and festive syntax highlighting.</p><p><strong>Labels:</strong> Theme Roller and variant dot tooltips now read \"Christmas Day ❄️\" / \"Christmas Eve 🎄\".</p>",
      "date": "2026-03-16"
    },
    {
      "year": "v0.19.5",
      "title": "Wedding - Classic Antique Gold Theme Palette",
      "description": "<p><strong>Feature Release:</strong> A new <strong>Wedding</strong> theme palette - two variants (Day + Evening) built around antique gold (<code>#C5A55A</code>) and silver tones, bringing the total theme count to 26.</p><p><strong>Wedding Theme:</strong></p><ul><li><strong>wedding-light (Day)</strong> - Warm cream backgrounds (<code>#FFFDF7</code>), antique gold primary (<code>#C5A55A</code>), antique silver secondary (<code>#A8A9AD</code>), dark warm text (<code>#2C2416</code>), warm tan borders (<code>#E8DCC8</code>), and warm gold-tinted shadows</li><li><strong>wedding-dark (Evening)</strong> - Deep midnight navy (<code>#12121F</code>), gold primary with lighter hover (<code>#D4B66A</code>), warm cream text (<code>#F0E8D8</code>), indigo borders (<code>#2A2A40</code>), and subtle gold-glow shadows</li></ul><p><strong>Registered across all surfaces:</strong> Available in the Theme Roller dropdown (Day ☀️ / Evening 🌙), the layout variant dot selector (count 24 → 26), the themes showcase card grid, the Kickstart Builder theme picker, and the <code>Domma.theme</code> API (<code>listThemes()</code> / <code>listBases()</code>).</p>",
      "date": "2026-03-16"
    },
    {
      "year": "v0.19.4",
      "title": "Mint - Fresh Spearmint Theme Palette",
      "description": "<p><strong>Feature Release:</strong> A new <strong>Mint</strong> theme palette - two variants (light + dark) built around cool spearmint and seafoam green tones, bringing the total theme count to 24.</p><p><strong>Mint Theme:</strong></p><ul><li><strong>mint-light</strong> - Rich eucalyptus primary (<code>#2e9e6e</code>), seafoam secondary (<code>#5b8a7a</code>), white surfaces with a subtle mint-tinted alt background (<code>#f0faf5</code>), near-black forest text (<code>#1a2e25</code>), and soft celadon borders (<code>#c8e6d8</code>)</li><li><strong>mint-dark</strong> - Deep slate-900 backgrounds with a dark mint-tinted surface (<code>#1e3a2f</code>), bright spearmint primary (<code>#6dd5a8</code>) for contrast, mint-tinted text (<code>#d4f5e4</code>), and dark teal borders (<code>#2a4a3a</code>)</li></ul><p><strong>Registered across all surfaces:</strong> Available in the Theme Roller dropdown, the layout variant dot selector (count 22 → 24), the themes showcase card grid, and the <code>Domma.theme</code> API (<code>listThemes()</code> / <code>listBases()</code>).</p>",
      "date": "2026-03-12"
    },
    {
      "year": "v0.19.3",
      "title": "Auto-Generated Breadcrumbs in the Layout System",
      "description": "<p><strong>Feature Release:</strong> The layout system now auto-generates frosted glass breadcrumb pills, overlaid top-left on hero sections and derived entirely from the page URL - no per-page configuration required.</p><p><strong>Layout System Breadcrumbs:</strong></p><ul><li><strong>Frosted glass pill</strong> - Semi-transparent black backdrop with <code>backdrop-filter: blur(6px)</code> sits at the top-left of every <code>.hero</code> section; falls back to a surface bar below the navbar on pages without a hero</li><li><strong>URL-driven label generation</strong> - Path segments are mapped through a built-in dictionary (<code>dom → DOM</code>, <code>http → HTTP</code>, <code>spa → SPA</code>, <code>theme-roller → Theme Roller</code>, etc.); anything unrecognised is title-cased automatically</li><li><strong>Title extraction</strong> - The last crumb's label is pulled from <code>&lt;title&gt;</code>, stripping common suffixes such as <code>· Domma Showcase</code>, for a precise page name</li><li><strong>Preset-driven</strong> - Enabled via <code>breadcrumbs.enabled: true</code> in <code>presets.json</code>; active for <code>showcase:subpage</code>, <code>showcase:effects</code>, <code>showcase:element</code>, and <code>public</code> presets; off for landing, index, minimal, admin, and demo pages</li><li><strong>Responsive</strong> - Pill shrinks at ≤576px (font-size 0.7 rem, tighter padding)</li><li><strong>Two-layer DOM</strong> - Outer <code>&lt;nav&gt;</code> holds the pill class; inner <code>&lt;div&gt;</code> is the Domma Breadcrumbs target, preserving component class isolation</li></ul><p><strong>Forms Fix:</strong> Checkbox fields no longer render a redundant label element alongside the input; the label is now handled by the checkbox markup itself.</p>",
      "date": "2026-03-06"
    },
    {
      "year": "v0.19.2",
      "title": "Forms - Checkbox Label Fix",
      "description": "<p><strong>Patch Release:</strong> Corrected label rendering logic in <code>forms.js</code> so that checkbox fields are excluded from the standard label output. Previously, a visible label element was injected alongside the checkbox even when the label was already embedded within the checkbox markup, causing doubled text on checkbox-type fields.</p>",
      "date": "2026-03-06"
    },
    {
      "year": "v0.19.1",
      "title": "Grayve - Slate & Steel Theme Palette",
      "description": "<p><strong>Feature Release:</strong> A new <strong>Grayve</strong> theme palette - two variants (light + dark) built around cool slate and steel-grey tones, bringing the total theme count to 22.</p><p><strong>Grayve Theme:</strong></p><ul><li><strong>grayve-light</strong> - Steel-blue primary, light slate surfaces, dark navy text, and soft grey borders; a neutral professional palette suited to data-heavy interfaces</li><li><strong>grayve-dark</strong> - Deep charcoal backgrounds, lighter steel primary for contrast, muted slate borders, and off-white text</li></ul><p><strong>Registered across all surfaces:</strong> Available in the Theme Roller dropdown, the layout variant dot selector (count 20 → 22), the Kickstart Builder theme picker, and the <code>Domma.theme</code> API.</p><p><strong>Breadcrumbs CSS Refactor:</strong> Inline styles previously injected by <code>elements.js</code> at runtime for <code>.dm-breadcrumbs-item</code>, <code>.dm-breadcrumbs-separator</code>, and <code>.dm-breadcrumbs-link</code> were moved to <code>elements.css</code> as static rules. The separator selector was also corrected from <code>.dm-breadcrumb-separator</code> (singular) to <code>.dm-breadcrumbs-separator</code> (plural) for consistency.</p>",
      "date": "2026-03-05"
    },
    {
      "year": "v0.19.0",
      "title": "Unicorn & Dreamy - Two New Theme Palettes",
      "description": "<p><strong>Feature Release:</strong> Two new fully theme-aware colour palettes - <strong>Unicorn</strong> (pink and purple) and <strong>Dreamy</strong> (beige and biscuit) - each with light and dark variants, bringing the total theme count to 20.</p><p><strong>Unicorn Theme:</strong></p><ul><li><strong>unicorn-light</strong> - Amethyst purple primary (<code>#9b59b6</code>), hot pink secondary (<code>#e91e90</code>), white surfaces with soft lavender tint (<code>#faf5fc</code>), dark aubergine text (<code>#2d1b3d</code>), and light purple borders (<code>#e1bee7</code>)</li><li><strong>unicorn-dark</strong> - Deep purple-black backgrounds (<code>#1a0e24</code> / <code>#241432</code>), lighter purple primary (<code>#ce93d8</code>) for contrast, lighter pink secondary (<code>#f48fb1</code>), and muted purple borders (<code>#4a2660</code>)</li></ul><p><strong>Dreamy Theme:</strong></p><ul><li><strong>dreamy-light</strong> - Warm brown primary (<code>#8d6e63</code>), warm cream surfaces (<code>#fffdf9</code> / <code>#f5f0eb</code>), dark chocolate text (<code>#3e2723</code>), and light biscuit borders (<code>#d7ccc8</code>)</li><li><strong>dreamy-dark</strong> - Dark espresso backgrounds (<code>#1c1410</code> / <code>#2a1f1a</code>), lighter taupe primary (<code>#bcaaa4</code>) for contrast, warm off-white text (<code>#efebe9</code>), and dark brown borders (<code>#4e342e</code>)</li></ul><p><strong>Registered across all surfaces:</strong> Both themes are available in the Theme Roller dropdown, the layout variant dot selector, the Kickstart Builder theme picker, and the <code>Domma.theme</code> API (<code>listThemes()</code> / <code>listBases()</code>). The variant dot count in the layout controls is updated from 16 to 20.</p><p><strong>Codebase Cleanup:</strong> Removed six stale backup and <code>.bak</code> files (<code>dom.js.backup-*</code>, <code>elements.js.backup-xss-*</code>, three celebration theme <code>.bak</code> files), the one-off <code>debug-wizard.html</code>, <code>firebase-debug.log</code>, the auto-generated <code>coverage/</code> directory, and the <code>.playwright-mcp/</code> screenshot cache.</p>",
      "date": "2026-03-04"
    },
    {
      "year": "v0.18.1",
      "title": "Canonical Sweep - SEO, Dead Links & Theme Fixes",
      "description": "<p><strong>Maintenance Release:</strong> Resolved Google Search Console's 'duplicate without user-selected canonical' warnings, rebuilt the sitemap from scratch, fixed five dead links across the public site, and corrected theme-awareness in the Blueprint showcase.</p><p><strong>Canonical Tags (119 pages):</strong> New <code>scripts/add-canonical-tags.js</code> inserts <code>&lt;link rel=\"canonical\"&gt;</code> after <code>&lt;/title&gt;</code> in every public HTML page. Root <code>index.html</code> maps to the bare domain URL; subdirectory <code>index.html</code> files use trailing-slash directory URLs; non-index <code>.html</code> files keep their extension. Template fragments (<code>layouts/templates/*</code>, <code>examples/*/template.html</code>) are skipped. The script is idempotent - safe to re-run.</p><p><strong>Sitemap Rebuild:</strong> New <code>scripts/rebuild-sitemap.js</code> walks <code>public/</code> and generates a fresh <code>sitemap.xml</code> from files that actually exist on disk. The previous sitemap contained 30+ phantom URLs; the new one has 101 real entries with priority tiers (1.0 root → 0.8 hubs → 0.7 module pages → 0.6 sub-pages → 0.3 legal). Also idempotent.</p><p><strong>Dead Link Fixes (5):</strong></p><ul><li><strong>effects/javascript.html</strong> - <code>typewriter.html</code> → <code>scribe.html</code> (effect was renamed in v0.14.x)</li><li><strong>elements/timer</strong> - 'Progress Bar' / <code>../progress/</code> → 'Progression' / <code>../progression/</code> (component renamed in v0.14.0)</li><li><strong>miniapps/address</strong> - <code>/docs</code> → <code>/api/</code> (documentation moved)</li><li><strong>nav-showcase.json</strong> - Removed phantom 'Invoicing System' entry (miniapp not yet built)</li><li><strong>download page</strong> - Removed QuickStart Starter card, QuickStart Templates section, and dead <code>quickstart-latest.json</code> <code>fetch()</code> call; all referenced archives are missing</li></ul><p><strong>Blueprint Showcase Theme Fix:</strong> The example areas (result panes, model state panels, blueprint method badges) were referencing non-existent CSS variables (<code>--dm-bg-secondary</code>, <code>--dm-bg-tertiary</code>) that fell back to hardcoded light-mode hex values, making them appear with light grey backgrounds in every dark theme. Fixed to use the correct Domma variables: <code>--dm-surface-raised</code>, <code>--dm-surface</code>, <code>--dm-primary-bg</code>, and <code>--dm-primary</code>. The code block also gained <code>var(--dm-code-bg)</code> so it adapts in dark themes.</p>",
      "date": "2026-02-25"
    },
    {
      "year": "v0.18.0",
      "title": "DommaComponents - Web Components with Shadow DOM",
      "description": "<p><strong>Major Release:</strong> Introducing <strong>DommaComponents</strong> - a first-class Web Components system built into the Domma framework. Define reusable, self-contained UI components with Shadow DOM encapsulation, reactive state, lifecycle hooks, and scoped styles, all using a clean declarative API.</p><p><strong>Domma.component() API:</strong></p><ul><li><strong>Custom element registration</strong> - <code>Domma.component('my-widget', { ... })</code> registers a native Web Component</li><li><strong>Shadow DOM encapsulation</strong> - Component markup and styles are fully isolated from the host page</li><li><strong>Reactive data</strong> - <code>data()</code> function defines component state; <code>onUpdated()</code> fires on every change</li><li><strong>Computed properties</strong> - Derived values that update automatically when data changes</li><li><strong>Methods</strong> - Event handler functions with access to <code>this.data</code>, <code>this.root</code>, and all Domma APIs</li><li><strong>Props with validation</strong> - Type-checked, default-valued attributes: <code>props: { initialMode: { type: M.types.string, default: 'basic' } }</code></li><li><strong>Scoped styles</strong> - CSS defined in the <code>style</code> block is injected into the Shadow DOM, invisible to the outside</li><li><strong>External templates</strong> - <code>templateUrl</code> works inside components for substantial markup</li><li><strong>Lifecycle hooks</strong> - <code>onBeforeMount</code>, <code>onMount</code>, <code>onUpdated</code>, <code>onUnmount</code></li></ul><p><strong>Four Example DommaComponents:</strong></p><ul><li><strong>&lt;domma-calculator&gt;</strong> - Scientific calculator with basic/scientific modes, memory functions, angle toggle, and full keyboard support</li><li><strong>&lt;domma-todo&gt;</strong> - Full CRUD task manager with priorities, status tracking, search, and localStorage persistence</li><li><strong>&lt;domma-markdown-editor&gt;</strong> - Real-time markdown editor with live preview pane, syntax helpers, auto-save, and keyboard shortcuts</li><li><strong>&lt;domma-notes&gt;</strong> - Rich note manager with categories, full-text search, overlay editor, and persistent storage</li></ul><p>Each component is a single <code>component.js</code> file with a companion <code>template.html</code>, downloadable from the Examples pages and droppable into any project with a single tag.</p><p><strong>Components Showcase:</strong> New dedicated showcase at <code>/showcase/components/</code> with live demos, API reference, pattern documentation, and a full tutorial covering all component features.</p><p><strong>Splash Page - Examples Row:</strong> Eight example tiles now appear on the landing page above MiniApps, each showing a description via Scribe typewriter animation on hover.</p>",
      "date": "2026-02-20"
    },
    {
      "year": "v0.17.0",
      "title": "Theme Renaissance - Complete Theming Overhaul",
      "description": "<p><strong>Major Release:</strong> Complete rebuild of the theming system achieving perfect theme-readiness across all 16 themes with full persistence support.</p><p><strong>Theme Engine Persistence:</strong></p><ul><li><strong>Auto-save/load</strong> - Theme selections now persist across sessions and automatically load on page refresh</li><li><strong>Fixed initialization</strong> - Removed explicit theme parameters that were overriding stored preferences</li><li><strong>Seamless continuity</strong> - Users' theme choices respected throughout the application</li></ul><p><strong>Semantic Variable Migration:</strong></p><ul><li>Replaced ~298 palette variables (<code>--dm-gray-*</code>, <code>--dm-slate-*</code>) with semantic theme variables</li><li>Components now use <code>--dm-text</code>, <code>--dm-surface</code>, <code>--dm-border</code>, <code>--dm-hover-bg</code></li><li>Semantic variables automatically adapt to each theme's color scheme</li></ul><p><strong>Unified Dark Mode Detection:</strong></p><ul><li>Introduced <code>data-mode</code> attribute set by theme engine</li><li>Replaced 78+ inconsistent selectors with unified <code>[data-mode=\"dark\"]</code></li><li>Consistent dark mode styling across entire component library</li></ul><p><strong>Theme File Corrections:</strong></p><ul><li>Fixed ocean-light/ocean-dark and forest-light/forest-dark file content swaps</li><li>Improved contrast in 5 dark themes (charcoal, silver, lemon, christmas, sunset)</li><li>Converted opaque status colors to RGBA tints for better dark theme integration</li></ul><p><strong>Component Enhancements:</strong></p><ul><li><strong>Navbar & Footer:</strong> Dark hover backgrounds (<code>#1e293b</code>) with white text in light mode</li><li><strong>Rounded corners:</strong> Changed hover border-radius from <code>sm</code> to <code>md</code> for more polished appearance</li><li><strong>Visual borders:</strong> Added 1px borders to navbar/footer for clear separation</li><li><strong>Card headers:</strong> Improved dark mode contrast with darker backgrounds</li></ul><p><strong>Bug Fixes:</strong></p><ul><li>Fixed splash hero, login tabs, navbar branding, and cookie consent theming</li><li>Corrected misuse of <code>--dm-text-inverse</code> variable</li><li>All 298+ palette variable references replaced with theme-adaptive semantic variables</li></ul><p><strong>Impact:</strong> Zero text visibility issues. All components work perfectly across all 16 themes with proper contrast and readability.</p>",
      "date": "2026-02-18"
    },
    {
      "year": "v0.16.0",
      "title": "Template Evolution - External Template Loading",
      "description": "<p><strong>Major enhancement to the SPA Router:</strong> Views can now load HTML templates from external files using <code>templateUrl</code> and <code>partials</code>, dramatically improving code organisation and maintainability.</p><p><strong>Key Features:</strong></p><ul><li><strong>External Templates</strong> - Reference <code>.html</code> files instead of embedding large HTML strings in JavaScript</li><li><strong>Template Caching</strong> - Templates fetched once and cached per session for optimal performance</li><li><strong>Parallel Partials</strong> - Reusable template sections loaded concurrently</li><li><strong>Mustache Syntax</strong> - Full support for <code>{{variable}}</code>, <code>{{#if}}</code>, <code>{{#each}}</code>, and <code>{{> partial}}</code></li><li><strong>Organised Structure</strong> - Recommended <code>js/views/templates/</code> directory pattern</li></ul><p><strong>Impact:</strong> Views with 400+ lines of inline HTML reduced to ~50 lines of clean JavaScript. HTML files get proper syntax highlighting and editor support, while logic stays separate from presentation.</p><p><strong>Enhanced Kickstart Template:</strong> New Domma projects now include built-in convention enforcement (<code>CLAUDE.md</code>, memory files) for AI-assisted development with Claude Code, Cursor, and similar tools.</p><p><strong>Documentation:</strong> Comprehensive <em>Template Files</em> tutorial added to Router Showcase with 6 detailed sections covering all aspects of external template usage. Router now discoverable from the showcase hub.</p>",
      "date": "2026-02-15"
    },
    {
      "year": "v0.15.0",
      "title": "Motion Override & Effects Polish",
      "description": "<p><strong>Critical Fix:</strong> Resolved issue where <code>reveal()</code> and <code>ripple()</code> effects were disabled by CSS media queries even when JavaScript explicitly set <code>respectMotionPreference: false</code>.</p><p><strong>Solution:</strong> Added <code>data-force-animation</code> attribute system to override CSS <code>@media (prefers-reduced-motion: reduce)</code> queries. Showcase demo pages now display animations correctly regardless of system motion preferences.</p><p><strong>Code Cleanup:</strong> Updated effects showcase pages to use consistent <code>respectMotionPreference: false</code> configuration, removing obsolete variable references across 3 showcase files.</p>",
      "date": "2026-02-14"
    },
    {
      "year": "v0.14.5",
      "title": "Typewriter Effect with Action Queue",
      "description": "<p>Introduced <code>Domma.effects.typewriter()</code> with character-by-character text rendering, blinking cursor, and powerful action queue system.</p><p><strong>Features:</strong></p><ul><li>Cursor styles: caret, block, underline</li><li>Action queue: <code>render</code>, <code>wait</code>, <code>undoRender</code> actions</li><li>Per-character entrance effects: fade, bounce, glow</li><li>Partial text deletion and configurable looping</li><li>Full control: pause/resume/restart</li></ul><p><strong>Performance:</strong> GPU acceleration and hardware-accelerated animations for buttery-smooth 60fps rendering. Perfect for hero banners, animated intros, and dynamic text displays.</p>",
      "date": "2026-02-13"
    },
    {
      "year": "v0.14.1",
      "title": "Patron Saint Celebrations & Animation Polish",
      "description": "<p>Transformed UK patron saint celebrations with horizontal-drifting flower petals:</p><ul><li><strong>St. David's Day</strong> - Yellow daffodil petals</li><li><strong>St. George's Day</strong> - Red/white rose petals</li><li><strong>St. Andrew's Day</strong> - Purple heather petals</li><li><strong>St. Patrick's Day</strong> - Green clover petals</li></ul><p><strong>Bug Fixes:</strong></p><ul><li>Fixed critical St. David's Day animation freeze caused by canvas state corruption (missing <code>ctx.restore()</code>)</li><li>Replaced harsh X-shaped Saltire sparkles with elegant 4-pointed stars</li><li>Reduced Guy Fawkes Night initial particle spam with gradual spawning (25% start, builds to 100% over 20 seconds)</li></ul><p>All patron saint celebrations now feature realistic flutter animations and gentle vertical bobbing.</p>",
      "date": "2026-02-09"
    },
    {
      "year": "v0.14.0",
      "title": "CSS Architecture & Progression Component",
      "description": "<p><strong>Major CSS Restructure:</strong> Proper build process with separation of concerns:</p><ul><li><code>domma.css</code> - Base styles + utilities</li><li><code>grid.css</code> - Layout systems</li><li><code>elements.css</code> - 22 UI components</li></ul><p><strong>New Progression Component:</strong> Unified component with dual modes for event visualisation:</p><ul><li><strong>Timeline mode</strong> - Chronological events</li><li><strong>Roadmap mode</strong> - Status-driven milestones</li><li>5 status states: planned, in-progress, completed, delayed, cancelled</li><li>Interactive tooltips and responsive layouts</li></ul><p>Enhanced celebrations showcase with accurate release timelines. Improved CSS build workflow with <code>src/css</code> source files and <code>npm run build:css</code> automation.</p>",
      "date": "2026-02-08"
    },
    {
      "year": "v0.13.4",
      "title": "Enhanced Celebrations & Particle Fixes",
      "description": "<p><strong>Bug Fixes:</strong> Resolved 'Invalid particle values' errors across all celebration themes (Halloween, Guy Fawkes, Christmas, St. Patrick's, St. George's, St. David's, St. Andrew's) by ensuring proper <code>vx</code>, <code>vy</code>, <code>static</code>, <code>x</code>, <code>y</code> initialisations.</p><p><strong>Visual Enhancements:</strong></p><ul><li>Fixed Christmas snow rendering and witch's broomstick orientation</li><li>Implemented dynamic Batman logo display on the moon</li><li>Added procedural fork-lighting for Halloween theme</li><li>Visually overhauled Catherine Wheel for Guy Fawkes theme with fork-lighting for more realistic and dynamic effects</li></ul>",
      "date": "2026-02-08"
    },
    {
      "year": "v0.13.3",
      "title": "Polished SPA Templates & Dev Server",
      "description": "<p>Redesigned SPA template views with professional layouts matching the QuickStart demo.</p><p><strong>New Development Server:</strong> Added built-in <code>npx domma serve</code> command with:</p><ul><li>Live reload functionality</li><li>Automatic MPA/SPA project detection</li><li>Fixed template CSS paths</li></ul>",
      "date": "2026-02-05"
    },
    {
      "year": "v0.13.2",
      "title": "SPA Template Path Fixes",
      "description": "<p><strong>Critical Fix:</strong> Fixed broken CSS and JavaScript paths in SPA template that caused white screen errors after <code>npx domma init</code>.</p>",
      "date": "2026-02-05"
    },
    {
      "year": "v0.13.1",
      "title": "Development Server with npm Scripts",
      "description": "<p>Added npm scripts to generated projects for easier development workflow:</p><ul><li><code>npm start</code></li><li><code>npm run serve</code></li></ul><p>Server auto-prompts after <code>npx domma init</code>.</p>",
      "date": "2026-02-05"
    },
    {
      "year": "v0.13.0",
      "title": "Built-in Development Server",
      "description": "<p>Introduced <code>npx domma serve</code> command with:</p><ul><li>Optional live-server integration</li><li>Automatic MPA/SPA detection</li><li>Custom port support (default: 3096)</li></ul>",
      "date": "2026-02-05"
    },
    {
      "year": "v0.12.1",
      "title": "DommaJS Brand Refresh & Bug Fixes",
      "description": "<p><strong>Branding:</strong> Rebranded to <strong>DommaJS</strong> across splash page, navbar, and footer.</p><p><strong>Enhancements:</strong> Improved feature card animations with smooth breathing effect.</p><p><strong>Critical Fix:</strong> Fixed <code>backToTop()</code> bug that could destroy page body element when selector parameter was used.</p>",
      "date": "2026-02-04"
    },
    {
      "year": "v0.12.0",
      "title": "SPA Router & Ambient Effects",
      "description": "<p><strong>SPA Router:</strong> Hash-based routing system with:</p><ul><li>Middleware support for route guards</li><li>View lifecycle hooks (<code>onEnter</code>, <code>onMount</code>, <code>onLeave</code>)</li><li>Navigation events</li></ul><p><strong>Ambient Effects:</strong> CSS-only ambient background effects (rotating glows, floating blobs, waves) for stunning visual presentations without JavaScript.</p>",
      "date": "2026-02-04"
    },
    {
      "year": "v0.11.1",
      "title": "Address MiniApp",
      "description": "<p>New production MiniApp for UK address lookup with postcode search.</p><p><strong>Features:</strong></p><ul><li>ONS local data integration</li><li>Premium getAddress.io provider support</li><li>Bar charts for analytics</li><li>Star ratings for saved addresses</li></ul>",
      "date": "2026-02-02"
    },
    {
      "year": "v0.11.0",
      "title": "Schema Builder - Visual Blueprint Designer",
      "description": "<p>Build Domma Blueprints visually with drag-and-drop interface.</p><p><strong>Features:</strong></p><ul><li>21 field types</li><li>Live form preview with validation testing</li><li>12-column grid layout support</li><li>Multi-format export: JavaScript, JSON, TypeScript</li></ul>",
      "date": "2026-01-26"
    },
    {
      "year": "v0.10.1",
      "title": "Easier Project Setup",
      "description": "<p>New CLI tools and setup documentation make getting started faster.</p><p>Run the post-install script to configure your environment automatically.</p>",
      "date": "2026-01-23"
    },
    {
      "year": "v0.10.0",
      "title": "Blueprints - Define Once, Use Everywhere",
      "description": "<p><strong>Major Release:</strong> Introducing Blueprints for unified data schemas.</p><p>Define your data structure once and use it with:</p><ul><li>Models (reactive data)</li><li>Forms (auto-generation)</li><li>CRUD operations (automatic API integration)</li></ul>",
      "date": "2026-01-23"
    },
    {
      "year": "Jan 22-23",
      "title": "Build System Updates",
      "description": "<p>Updated distribution bundles with improved build process and version consistency across all bundle variants.</p>",
      "date": "2026-01-22"
    },
    {
      "year": "Jan 21",
      "title": "CDN Distribution Ready",
      "description": "<p>Framework now available via CDN for easier integration.</p><p>Simply include the script tag and start using Domma in your projects.</p>",
      "date": "2026-01-21"
    },
    {
      "year": "v0.9.x",
      "title": "Foundation Release",
      "description": "<p>Core framework with:</p><ul><li>DOM manipulation (jQuery-compatible API)</li><li>Utilities (Lodash-compatible)</li><li>Reactive models</li><li>UI components</li><li>Theming system</li></ul>",
      "date": "2025-12"
    }
  ],
  "latestVersion": "v0.34.0"
}
