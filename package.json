{
  "name": "domma",
  "version": "0.8.0b",
  "description": "Dynamic Object Manipulation & Modeling API - A complete front-end toolkit.",
  "main": "public/dist/domma.min.js",
  "module": "public/dist/domma.esm.js",
  "types": "assets/types/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:bundles && rollup -c && node scripts/build-info.js && npm run build:metadata && npm run build:css && live-server public --port=3000 --open=/index.html",
    "build": "npm run generate:bundles && rollup -c && node scripts/build-info.js && npm run build:metadata && npm run build:css",
    "build:js": "rollup -c",
    "build:css": "node scripts/build-css.js",
    "build:info": "node scripts/build-info.js",
    "build:metadata": "node scripts/copy-metadata.js",
    "generate:bundles": "node scripts/generate-bundles.js",
    "test": "node -r ./tests/setup-globals.js tests/test.js",
    "showcase": "live-server public --port=3000 --open=/showcase/index.html"
  },
  "keywords": [
    "dom",
    "http",
    "utils",
    "toolkit",
    "json-config"
  ],
  "author": "Darryl Waterhouse",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.3.0",
    "@rollup/plugin-replace": "^6.0.3",
    "@rollup/plugin-terser": "^0.4.4",
    "live-server": "^1.2.0",
    "rollup": "^4.28.0",
    "rollup-plugin-obfuscator": "^1.1.0",
    "jsdom": "^24.1.0"
  }
}
